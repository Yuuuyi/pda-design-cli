# 反模式 (Anti-Patterns)

这些错误会破坏 PDA 规范：

1. **绝不使用生硬的阴影** — 仅使用极低透明度的 rgba 微妙阴影。纵深来自分层，而非突出的投影。
2. **绝不使用纯黑** (`#000000`)
3. **模块间隔颜色绝不是白色** — 使用 `#EEEEEE` 作为间隔色
4. **绝不使用快速动画** — 交互至少 500ms，图片至少 1500-2000ms。
5. **绝不使用非组件库色彩**
6. **绝不使用非组件库字体及非组件库字号**
7. **绝不添加渐变效果**
