# 无障碍访问 (Accessibility) 考量

## 动效偏好

- 尊重 `prefers-reduced-motion` 设置，照顾前庭功能障碍用户。
- 将动画持续时间减至 0ms 或使用更简单的过渡。
- 保留颜色变化，但移除变换和缩放。

## 排版

- 大号正文 (20px 基准) 确保可读性。
- 主文本高对比度。
- 避免两端对齐文本 — 使用左对齐。

## 交互区域

- 按钮最小高度 48px (`h-12`)，符合触控目标标准。
- 充足的内边距创造更大的可点击区域。
- 交互元素间的间距防止误触。
